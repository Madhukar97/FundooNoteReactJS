import React, { Component } from "react";
import Header from "./Header";
import CreateArea from "./CreateArea"

const Home = () => {
  return (
		<>
		<Header/>
		<CreateArea/>
		</>
	);
};

export default Home;
